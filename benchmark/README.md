# Benchmark Results

This directory is reserved for storing benchmark output files (e.g., JSON reports, comparison tables) generated by running the temporal memory benchmark suite.

To generate results:
```bash
pytest tests/test_benchmark.py -v
```

See the [pytest plugin docs](../README.md#pytest-benchmark-plugin) for how to benchmark third-party memory backends using the adapter interface.
